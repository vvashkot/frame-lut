[phases.setup]
nixPkgs = ["nodejs_22", "ffmpeg"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npm run build",
  "npx tsx scripts/importLUTs.ts luts/",
  "echo 'LUTs imported successfully'",
  "ffmpeg -version"
]

[start]
cmd = "npm start"